import { defaultConfig } from "@/config/default";

// Calculate Application positions
const calculate_application_positions = (positions, idx) => {
    return positions ?? { x: 0, y: 0 };
};

export { calculate_application_positions };
