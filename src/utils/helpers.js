import { defaultConfig } from "@/config/default";

// Bellow Function checks if the given Width and Height is possible to be a OS screen
// const isPossibleScreen = (width, height) => {
//     if (
//         height > width ||
//         width / height < defaultConfig.aspectRatio.min ||
//         width / height > defaultConfig.aspectRatio.max
//     ) {
//         return false;
//     }

//     return true;
// };

// export { isPossibleScreen };
